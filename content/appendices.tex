\clearpage

\setcounter{chapter}{6}
\setcounter{section}{0}

% Add the appendices to table of contents
\addcontentsline{toc}{chapter}{\numberline{A}Appendices}

% Set up page style for appendices
\pagestyle{fancy}
\fancyhf{} % Clear all header and footer fields
\fancyhead[L]{\footnotesize\textit{Ars Post Faber: Digital fabrication democratization through embodied knowledge preservation}}
\fancyfoot[C]{\thepage} % Page number in footer center
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Custom appendices title to match chapter formatting
\noindent
{\Large\textbf{Appendices}}
\vspace{0.3cm}
\hrule
\vspace{0.8cm}
\label{ch:appendices}

% Set no paragraph indentation
\setlength{\parindent}{0pt}

\section{\textit{AI.RTISANSHIP} technical documentation}

\textit{AI.RTISANSHIP} experiment represented an attempt to capture and digitize traditional pottery techniques through computer vision and machine learning. This technical documentation outlines the implementation pipeline, from data collection through real-time prediction deployment.

\vspace{0.5cm}

The code for this project can be found opened under an MIT license on the following GitHub repository: \href{https://github.com/jmuozan/ML_Sequence_Recognition}{github.com/jmuozan/ML\_Sequence\_Recognition}

\subsection{System architecture overview}

The system implemented a machine learning system organized into seven main sections within a Jupyter notebook environment (\texttt{ML\_Sequence\_Recognition.ipynb}), progressively building from data collection to real-time prediction capabilities.

Technology stack:
\begin{enumerate}
\item MediaPipe for holistic landmark detection (pose and hand tracking)
\item OpenCV for computer vision and video processing
\item TensorFlow/Keras for bidirectional LSTM neural network training
\item Pandas and NumPy for data processing and feature extraction
\end{enumerate}

This system captures 225-dimensional feature vectors from pose and hand landmarks, processes sequences through LSTM networks, and provides real-time feedback about movement correctness.

\subsection{Data collection and MediaPipe integration}

\subsubsection{Library setup and MediaPipe configuration}

MediaPipe's holistic model ids used to do a simultaneous pose and hand tracking.

\begin{verbatim}
import mediapipe as mp
import cv2
import numpy as np
import pandas as pd
from datetime import datetime

mp_pose = mp.solutions.pose
mp_drawing = mp.solutions.drawing_utils

cap = cv2.VideoCapture(0)
with mp_pose.Pose(min_detection_confidence=0.5, 
                  min_tracking_confidence=0.5) as pose, \
    mp.solutions.hands.Hands(min_detection_confidence=0.5, 
                            min_tracking_confidence=0.5) as hands:
    
    while cap.isOpened():
        ret, frame = cap.read()
        if not ret:
            break
            
        # Convert BGR to RGB for MediaPipe
        image = cv2.cvtColor(frame, cv2.COLOR_BGR2RGB)
        image.flags.writeable = False
        
        # Make detections
        pose_results = pose.process(image)
        hand_results = hands.process(image)
        
        # Convert back to BGR for display
        image.flags.writeable = True
        image = cv2.cvtColor(image, cv2.COLOR_RGB2BGR)
\end{verbatim}

The detection confidence thresholds of 0.5 looks to balance accuracy and speed, maintaining consistency across frames.

\subsubsection{Feature extraction system}

As mentioned, the system extracts 225-dimensional feature vectors from each frame, creating a representation of body posture and hand positions for each video frame

\begin{verbatim}
def extract_keypoints(results):
    # Extract pose landmarks (33 points x 3 coordinates = 99 features)
    pose = np.array([[res.x, res.y, res.z] for res in 
                     results.pose_landmarks.landmark]).flatten() \
           if results.pose_landmarks else np.zeros(33*3)
    
    # Extract left hand landmarks (21 points x 3 coordinates = 63 features)
    lh = np.array([[res.x, res.y, res.z] for res in 
                   results.left_hand_landmarks.landmark]).flatten() \
         if results.left_hand_landmarks else np.zeros(21*3)
    
    # Extract right hand landmarks (21 points x 3 coordinates = 63 features)
    rh = np.array([[res.x, res.y, res.z] for res in 
                   results.right_hand_landmarks.landmark]).flatten() \
         if results.right_hand_landmarks else np.zeros(21*3)
    
    return np.concatenate([pose, lh, rh])
\end{verbatim}

\subsubsection{Semi-automated data labeling}

The data collection implements a semiautomatic labeling based on a keyboard key controlled system for movement sequences, allowing for a "real-time" labeling.

\begin{verbatim}
# Recording control system
key = cv2.waitKey(10) & 0xFF
if key == ord('r'):  # Start recording correct sequence
    recording = True
    current_label = 'correct'
elif key == ord('w'):  # Start recording incorrect sequence
    recording = True
    current_label = 'incorrect'
elif key == ord('s'):  # Stop recording
    recording = False
elif key == ord('q'):  # Quit application
    break
\end{verbatim}

\subsection{Data processing and neural network training}

\subsubsection{Data consolidation and labeling}

After labeling multiple recording sessions, these are combined into a unified training dataset.

\begin{verbatim}
# Combine multiple data collection sessions
df1 = pd.read_csv('coordinates_1.csv', header=None)
df2 = pd.read_csv('coordinates_2.csv', header=None) 
combined_df = pd.concat([df1, df2], axis=0, ignore_index=True)

# Transform binary accuracy to readable labels
data['accuracy'] = data['accuracy'].map({0: 'W', 1: 'R'})  # Wrong/Right

# Create composite labels (e.g., "pottery_R", "pottery_W")
data['label'] = data.iloc[:, 0].astype(str) + '_' + data['accuracy']
\end{verbatim}

\subsubsection{LSTM neural network architecture}

The classification system uses a three-layer bidirectional LSTM architecture for temporal sequence recognition:

\begin{verbatim}
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import LSTM, Dense, Dropout, Bidirectional
from tensorflow.keras.regularizers import l2
from tensorflow.keras.optimizers import Adam

# Build bidirectional LSTM architecture
model = Sequential([
    # First bidirectional layer with return sequences
    Bidirectional(LSTM(64, return_sequences=True, activation='tanh'), 
                  input_shape=(sequence_length, 225)),
    Dropout(0.2),
    
    # Second bidirectional layer with L2 regularization
    Bidirectional(LSTM(128, return_sequences=True, activation='tanh', 
                       kernel_regularizer=l2(0.01))),
    Dropout(0.2),
    
    # Final bidirectional layer
    Bidirectional(LSTM(64, activation='tanh')),
    
    # Dense classification layers
    Dense(64, activation='relu'),
    Dense(32, activation='relu'),
    Dense(num_classes, activation='softmax')
])

# Compile model with Adam optimizer
optimizer = Adam(learning_rate=0.001)
model.compile(optimizer=optimizer, 
              loss='categorical_crossentropy', 
              metrics=['accuracy'])
\end{verbatim}

This bidirectional LSTM processes sequences in both forward and backward directions, capturing temporal dependencies in movement patterns.

\subsubsection{Training configuration with callbacks}

Training uses callbacks for learning control and model optimization:

\begin{verbatim}
from tensorflow.keras.callbacks import EarlyStopping, ModelCheckpoint, ReduceLROnPlateau

# Configure training callbacks
early_stopping = EarlyStopping(monitor='val_accuracy', 
                               patience=20, 
                               restore_best_weights=True)

checkpoint = ModelCheckpoint('best_model.keras', 
                            save_best_only=True, 
                            monitor='val_accuracy', 
                            mode='max')

reduce_lr = ReduceLROnPlateau(monitor='val_loss', 
                             factor=0.2, 
                             patience=5, 
                             min_lr=0.0001)

# Execute training with monitoring
history = model.fit(X_train, y_train, 
                   validation_split=0.2, 
                   epochs=2000, 
                   batch_size=32,
                   callbacks=[early_stopping, checkpoint, reduce_lr])
\end{verbatim}

\subsection{Real-time prediction system}

\subsubsection{Live processing pipeline}

To test the results of the training a real-time detection system is created, integrating MediaPipe detection with the trained model inference:

\begin{verbatim}
def mediapipe_detection(image, model):
    # Process frame through MediaPipe pipeline
    image = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)
    image.flags.writeable = False
    results = model.process(image)
    image.flags.writeable = True
    image = cv2.cvtColor(image, cv2.COLOR_RGB2BGR)
    return image, results

def draw_styled_landmarks(image, results, incorrect_movement):
    # Render landmarks with correctness-based color coding
    color = (0, 0, 255) if incorrect_movement else (0, 255, 0)  # Red/Green
    
    # Draw pose landmarks
    if results.pose_landmarks:
        mp_drawing.draw_landmarks(
            image, results.pose_landmarks, mp_pose.POSE_CONNECTIONS,
            mp_drawing.DrawingSpec(color=color, thickness=2),
            mp_drawing.DrawingSpec(color=color, thickness=2))
\end{verbatim}

The system provides visual feedback through color-coded landmark rendering - green for correct movements, red for incorrect ones.

\subsubsection{Rolling buffer for temporal context}

This real-time prediction system maintains temporal context through a frame buffer capturing and saving a set of frames and rewriting it every frame so the prediction is done over the frame collection.

\begin{verbatim}
# Initialize system components
sequence = []
cap = cv2.VideoCapture(0)
model = load_model('pottery_model.h5')

with mp_holistic.Holistic(min_detection_confidence=0.5, 
                          min_tracking_confidence=0.5) as holistic:
    while cap.isOpened():
        ret, frame = cap.read()
        frame = cv2.flip(frame, 1)  # Mirror image
        
        # Process frame and extract keypoints
        image, results = mediapipe_detection(frame, holistic)
        
        if results.pose_landmarks or results.left_hand_landmarks or results.right_hand_landmarks:
            keypoints = extract_keypoints(results)
            sequence.append(keypoints)
            sequence = sequence[-30:]  # Maintain 30-frame rolling window
            
            # Predict when buffer is full
            if len(sequence) == 30:
                res = model.predict(np.expand_dims(sequence, axis=0))[0]
                action = actions[np.argmax(res)]
                incorrect_movement = action.endswith("_W")
                
                # Visual feedback
                draw_styled_landmarks(image, results, incorrect_movement)
                color = (0, 0, 255) if incorrect_movement else (0, 255, 0)
                cv2.putText(image, 'Action: ' + str(action), (15, 50), 
                           cv2.FONT_HERSHEY_SIMPLEX, 1, color, 2)
        
        cv2.imshow('AI.RTISANSHIP', image)
        if cv2.waitKey(10) & 0xFF == ord('q'):
            break
\end{verbatim}

\subsection{Technical limitations and research insights}

This implementation showcased certain limitations that informed already mentioned research conclusions:

\subsubsection{Sensory constraints}
Computer vision systems remained blind to tactile feedback channels that limit the use of the trained model.

\subsubsection{Individual variation and resistance to standardization}
Each participating artisan approached identical tasks differently, reflecting experience, histories and preferences that resist standardization.

These limitations demonstrated that while the technical implementation worked as designed, the approach of digitizing embodied knowledge through movement classification "misunderstood" the nature of craft practice.

\clearpage
\section{\textit{CR3ATED} Technical documentation}

The \textit{CR3ATED} web application was developed to test alternative interface approaches for preserving creative agency within digital fabrication workflows. This technical documentation outlines the implementation of the 3D revolution modeling webapp with focus on its main capabilities: 3D generation from drawings, image-based surface modification, and displacement mapping.

\vspace{0.5cm}

The complete source code is available as open-source under MIT License at \href{https://github.com/jmuozan/CR3ATED}{github.com/jmuozan/CR3ATED}, and a live demonstration can be accessed at \href{https://jmuozan.github.io/CR3ATED/}{jmuozan.github.io/CR3ATED}.

\subsection{System architecture overview}

The web application implements a complete 3D modeling pipeline using web technologies, focusing on revolution surface generation and mesh processing capabilities. Its modular architecture divides functionalities across specific JavaScript modules for maintainability.

\vspace{0.5cm}

Core technology stack:
\begin{enumerate}
\item Three.js for 3D rendering and scene management
\item WebGL-accelerated graphics through Three.js WebGLRenderer
\item Custom Loop Subdivision implementation for mesh smoothing
\item HTML5 Canvas API for image processing and drawing interfaces
\end{enumerate}

\vspace{0.5cm}

The webapp supports STL import/export, image-based displacement mapping, interactive mesh subdivision, and real-time 3D manipulation through touch and mouse interfaces.

\subsection{3D scene initialization and rendering}

\subsubsection{Basic scene setup}

A core 3D environment is initiallized through a scene configuration that establishes the WebGL rendering context and user interaction capabilities:

\begin{verbatim}
export function initializeScene() {
    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(75, 
        window.innerWidth / window.innerHeight, 0.1, 1000);
    camera.position.z = 5;

    const renderer = new THREE.WebGLRenderer({ antialias: true });
    renderer.setSize(window.innerWidth, window.innerHeight);
    renderer.setClearColor(0xffffff);
    document.body.appendChild(renderer.domElement);

    const controls = new THREE.OrbitControls(camera, renderer.domElement);

    // Setup lighting
    setupLights(scene);

    return { scene, camera, renderer, controls };
}
\end{verbatim}

This scene initialization creates a THREE.Scene object as the root container for all 3D objects.

\subsubsection{Lighting configuration}

In order to ensure consistent visibility from all viewing angles of the 3d object, the web application implements multiple light sources.

\begin{verbatim}
function setupLights(scene) {
    const ambientLight = new THREE.AmbientLight(0x404040, 2);
    scene.add(ambientLight);

    const directionalLight = new THREE.DirectionalLight(0xffffff, 0.5);
    directionalLight.position.set(0, 5, 5);
    scene.add(directionalLight);

    // Additional directional lights for full coverage
    const directionalLight1 = new THREE.DirectionalLight(0xffffff, 0.5);
    directionalLight1.position.set(-5, 5, 5);
    scene.add(directionalLight1);

    const pointLight = new THREE.PointLight(0xffffff, 0.5);
    pointLight.position.set(0, 0, 20);
    scene.add(pointLight);
}
\end{verbatim}

\subsection{Drawing interface for 3D generation}

\subsubsection{Modal-based drawing system}

The application includes an integrated drawing interface for users to create revolution surfaces through finger drawing on smartphones or mouse drawing on desktop:

\begin{verbatim}
function initDrawingBoard() {
    const modalHTML = `
        <div id="drawingModal" class="modal">
            <div class="modal-content">
                <span class="close">&times;</span>
                <div class="controls">
                    <button id="penTool" class="active">✏️ Pen</button>
                    <div class="control-group">
                        <label>Points Density: </label>
                        <input type="range" id="pointDensity" min="1" max="20" value="5">
                        <span id="densityValue">5px</span>
                    </div>
                    <button id="clearCanvas">Clear</button>
                    <button id="preview3D">3D Preview</button>
                    <button id="exportSTL">Export STL</button>
                </div>
                <div class="canvas-container">
                    <svg id="drawing-area" viewbox="0 0 550 400">
                        <line x1="275" y1="0" x2="275" y2="400" 
                              stroke="#666" stroke-width="1" stroke-dasharray="5,5">
                        </line>
                    </svg>
                </div>
            </div>
        </div>
    `;
    
    document.body.insertAdjacentHTML('beforeend', modalHTML);
}
\end{verbatim}

The drawing interface creates a popup with SVG-based drawing area. Users can draw profiles that are automatically converted into 3D revolution surfaces through a connection of the 2D coordinates.

\subsection{Mesh management}

\subsubsection{STL import functionality}

The application also provides STL file loading capabilities to allow user bring and modify their own models.

\begin{verbatim}
export function handleSTLUpload(params) {
    const file = params.event.target.files[0];
    if (file) {
        const reader = new FileReader();
        
        reader.onload = function (e) {
            const loader = new THREE.STLLoader();
            const geometry = loader.parse(e.target.result);

            if (params.mesh) {
                params.scene.remove(params.mesh);
                params.scene.remove(params.wireframe);
            }

            geometry.computeVertexNormals();
            
            // Store original positions for displacement operations
            geometry.originalPositions = geometry.attributes.position.array.slice();

            const newMesh = new THREE.Mesh(geometry, params.solidMaterial);
            const newWireframe = new THREE.Mesh(geometry.clone(), params.wireframeMaterial);
            newWireframe.visible = false;

            // Default vertical orientation
            newMesh.rotation.x = -Math.PI / 2;
            newWireframe.rotation.x = -Math.PI / 2;

            params.scene.add(newMesh);
            params.scene.add(newWireframe);
        };
        reader.readAsArrayBuffer(file);
    }
}
\end{verbatim}

The import process uses Three.js STLLoader to parse binary or ASCII STL files. The system will automatically compute vertex normals for proper lighting and store vertex positions for posterior displacement operations.

\subsubsection{STL export capability}

The application also enables the STL export of modified geometries:

\begin{verbatim}
export function downloadSTL(currentMesh) {
    if (!currentMesh) return;
    
    const exporter = new THREE.STLExporter();
    const stlData = exporter.parse(currentMesh);

    const blob = new Blob([stlData], { type: 'text/plain' });
    const link = document.createElement('a');
    link.style.display = 'none';
    document.body.appendChild(link);
    link.href = URL.createObjectURL(blob);
    link.download = 'mesh.stl';
    link.click();
    document.body.removeChild(link);
}
\end{verbatim}

\subsection{Image-based displacement mapping}

\subsubsection{UV coordinate generation}

UV coordinate genration is responsable of creating the mapping between 2D texture space and 3D mesh surfaces for displacement operations:

\begin{verbatim}
export function generateUVs(geometry, displacementSettings) {
    const position = geometry.attributes.position;
    const box = new THREE.Box3().setFromBufferAttribute(position);
    const size = new THREE.Vector3();
    box.getSize(size);

    const uv = new THREE.BufferAttribute(new Float32Array(position.count * 2), 2);
    const positionArray = position.array;

    for (let i = 0; i < position.count; i++) {
        const x = positionArray[i * 3];
        const y = positionArray[i * 3 + 1];
        const z = positionArray[i * 3 + 2];
        
        // Project to XZ plane for UV mapping
        let uvX = (x - box.min.x) / size.x;
        let uvY = (z - box.min.z) / size.z;
        
        if (displacementSettings.flipUV) {
            uvX = 1 - uvX;
            uvY = 1 - uvY;
        }
        
        uv.setXY(i, uvX, uvY);
    }

    geometry.setAttribute('uv', uv);
    return uv;
}
\end{verbatim}

This UV generation process calculates the bounding box of the mesh geometry and projects 3D vertices onto the XZ plane, creating a planar mapping that will allow the displacement operations.

\subsubsection{Displacement calculation and application}

Displacement implements real-time surface deformation based on image height data so mesh vertices can be moved according. to the mapping previousely done based on the desired image by the user.

\begin{verbatim}
export function applyDisplacementMap(geometry, texture, scale, displacementSettings) {
    if (!texture || !geometry) return geometry;
    
    geometry.computeVertexNormals();

    const position = geometry.attributes.position;
    const normals = geometry.attributes.normal;
    const uv = generateUVs(geometry, displacementSettings);
    const dispMap = texture.image;

    if (!dispMap) return geometry;

    // Create canvas for image data extraction
    const canvas = document.createElement('canvas');
    canvas.width = dispMap.width;
    canvas.height = dispMap.height;
    const context = canvas.getContext('2d');
    context.drawImage(dispMap, 0, 0);
    const imageData = context.getImageData(0, 0, dispMap.width, dispMap.height);
    const data = imageData.data;

    // Process displacement per vertex
    const positionArray = position.array;
    
    for (let i = 0; i < position.count; i++) {
        // Sample displacement from image
        const ux = Math.floor(uv.getX(i) * dispMap.width);
        const uy = Math.floor(uv.getY(i) * dispMap.height);
        const pixelIndex = (uy * dispMap.width + ux) * 4;
        const displacement = (data[pixelIndex] / 255) * scale;
        
        // Apply displacement along vertex normal
        const normalX = normals.getX(i);
        const normalY = normals.getY(i);
        const normalZ = normals.getZ(i);
        
        position.setXYZ(
            i,
            positionArray[i * 3] + normalX * displacement,
            positionArray[i * 3 + 1] + normalY * displacement,
            positionArray[i * 3 + 2] + normalZ * displacement
        );
    }

    position.needsUpdate = true;
    geometry.computeVertexNormals();
    return geometry;
}
\end{verbatim}

\subsection{Usage guidelines and capabilities}

\subsubsection{Drawing interface optimization}

For optimal results with the drawing interface, users should draw "clear", "smooth" profiles that define the silhouette of their desired revolution surface. The system will automatically handle the mathematical transformation from 2D profile to 3D revolution solid.

\subsubsection{Image displacement effectiveness}

For effective displacement mapping, users should use high-contrast images where lighter areas represent raised surfaces and darker areas represent recessed areas, and add higher levels of subdividion to the meshes, as that will define the amount of vertices tha move. The system works best with highquality grayscale images that provide clear height information.

\subsection{Technical limitations and considerations}

The implementation has several limitations. The displacement mapping depends on browser Canvas API support and may be computationally intensive for very large meshes. The drawing interface works best on touch devices but also supports mouse input for desktop users even though it might not be properly responsive.

Despite these limitations, the webapp successfully showcases an approach to 3D modeling through direct drawing interaction.

\clearpage
\section{\textit{AI Tools} Technical documentation}

\textit{AI Tools} plugin is an integration of artificial intelligence capabilities into parametric design workflows through Grasshopper. This technical documentation outlines the implementation of the two components that represent the plugin using external AI services to generate 3D models from text prompts and analyze images to create detailed modeling instructions.

\vspace{0.5cm}

The complete source code is available as open-source under MIT License at \href{https://github.com/jmuozan/AI_3D_Models_Grasshopper}{github.com/jmuozan/AI\_3D\_Models\_Grasshopper}, and the compiled plugin can be downloaded from \href{https://www.food4rhino.com/en/app/ai-tools}{Food4Rhino}.

\subsection{System architecture overview}

The plugin follows the Rhino Grasshopper plugin architecture and consists of two main components that integrate external AI services into traditional CAD workflows. The implementation combines Python scripts for AI service integration with C\# for Grasshopper component management.

\vspace{0.5cm}

Tech stack:
\begin{enumerate}
\item ZOO's Text-to-CAD API for natural language to 3D model generation
\item OpenAI's GPT-4 Vision API for image analysis and prompt generation
\item Python 3
\item C\# .NET Framework 4.8 
\item RhinoCommon API for geometry processing and file handling
\end{enumerate}

\vspace{0.5cm}

Plugin supports STEP, STL, and OBJ file formats, automatic file organization, and real-time integration within the Grasshopper visual programming environment, even though STEP files selected as default due to their superior compatibility with CAD software and preservation of parametric information.

\subsection{Text to CAD component implementation}

\subsubsection{Component architecture}

Text to CAD component integrates with ZOO's Text-to-CAD API to generate 3D models from natural language descriptions. The component receives four input parameters: API key for ZOO authentication, directory path for temporary file storage, text prompt describing the desired 3D model, and a boolean trigger to initiate generation. The outputs include the generated Brep geometry and the file path to the created STEP file.

\subsubsection{Python script implementation}

The core functionality is the KittyCAD API client use for integration with ZOO's machine learning services. This is how Text-to-CAD looks:
 
\begin{verbatim}
#! python 3
# r: kittycad
# requirements: kittycad

import time
import os
import rhinoscriptsyntax as rs
import Rhino
import shutil
import scriptcontext as sc
from datetime import datetime

from kittycad.api.ml import create_text_to_cad, get_text_to_cad_model_for_user
from kittycad.client import Client
from kittycad.models.api_call_status import ApiCallStatus
from kittycad.models.file_export_format import FileExportFormat
from kittycad.models.text_to_cad_create_body import TextToCadCreateBody

# API key initialization
API_KEY = api
client = Client(token=API_KEY)

# Output directory setup
output_dir = path
os.makedirs(output_dir, exist_ok=True)

# File format configuration
a = 2  # STEP format (most compatible)
run_code = generate 

if run_code:
    # Determine output format
    if a == 1:
        output_format = FileExportFormat.STL
        file_extension = "stl"
    elif a == 2:
        output_format = FileExportFormat.STEP
        file_extension = "step"
    elif a == 3:
        output_format = FileExportFormat.OBJ
        file_extension = "obj"
    else:
        raise ValueError("Invalid format selection")

    # Create text-to-CAD request
    response = create_text_to_cad.sync(
        client=client,
        output_format=output_format,
        body=TextToCadCreateBody(
            prompt=prompt,
        ),
    )

    # Poll for completion
    while response.completed_at is None:
        time.sleep(5)
        response = get_text_to_cad_model_for_user.sync(
            client=client,
            id=response.id,
        )

    # Handle response
    if response.status == ApiCallStatus.FAILED:
        print(f"Text-to-CAD failed: {response.error}")
    elif response.status == ApiCallStatus.COMPLETED:
        print(f"Text-to-CAD completed with {len(response.outputs)} files")
        
        # Save generated file
        final_result = response.outputs[f"source.{file_extension}"]
        output_file_path = os.path.join(output_dir, 
            f"text-to-cad-output.{file_extension}")
        
        with open(output_file_path, "wb") as output_file:
            output_file.write(final_result)
            print(f"Saved to {output_file_path}")
\end{verbatim}

In order to handle the asynchronous output of machine learning model inference, the script implements a polling, checking completion status every five seconds until the model generates results.

\subsubsection{File import and geometry processing}

The generated STEP files are automatically imported into Rhino and processed into Brep objects through the following file handling:

\begin{verbatim}
def import_step_file(step_file_path):
    """Import STEP file and return imported objects"""
    if not isinstance(step_file_path, str):
        print("Invalid path:", step_file_path)
        return []

    if not os.path.exists(step_file_path):
        print("File does not exist:", step_file_path)
        return []

    # Import using Rhino command
    rs.Command(f"_-Import \"{step_file_path}\" _Enter", False)
    
    # Wait for import completion
    time.sleep(2)  
    sc.doc.Views.Redraw()

    # Configure object enumeration
    from Rhino.DocObjects import ObjectEnumeratorSettings
    settings = ObjectEnumeratorSettings()
    settings.IncludeLights = False
    settings.IncludeGrips = False
    settings.IncludeAnnotations = False
    settings.IncludeInstances = False
    settings.IncludeDetails = False
    settings.IncludeHatchPatterns = False
    settings.IncludeMeshes = False
    settings.IncludeCurves = False
    settings.IncludePoints = False
    settings.IncludeSurfaces = False
    settings.IncludeBrep = True  

    # Retrieve imported objects
    imported_objects = list(Rhino.RhinoDoc.ActiveDoc.Objects.GetObjectList(settings))
    print(f"Found {len(imported_objects)} objects in the document.")

    # Clean up - delete from document but keep references
    for obj in imported_objects:
        Rhino.RhinoDoc.ActiveDoc.Objects.Delete(obj, True)  

    return imported_objects
\end{verbatim}

This import process uses Rhino's native command interface to ensure compatibility with all supported file formats in case the user decides to change them.

\subsubsection{Automatic file organization}

In order to see the different interations and have them organzed, the system includes automatic file organization with versioning and timestamping:

\begin{verbatim}
def move_and_rename_file(step_file_path):
    """Move and rename generated files with versioning"""
    main_directory = os.path.dirname(step_file_path)
    models_folder = os.path.join(main_directory, "MODELS")

    # Create models directory if needed
    if not os.path.exists(models_folder):
        os.makedirs(models_folder)
        print(f"Created folder: {models_folder}")

    # Generate version number
    existing_files = [f for f in os.listdir(models_folder) 
                     if f.endswith('.step') or f.endswith('.stp')]
    model_number = len(existing_files) + 1

    # Create timestamped filename
    current_time = datetime.now().strftime("%Y-%m-%d_%H-%M")
    file_name = f"model-{model_number}_{current_time}.step"
    destination_path = os.path.join(models_folder, file_name)

    # Move file
    shutil.move(step_file_path, destination_path)
    print(f"Moved and renamed file to: {destination_path}")
\end{verbatim}

This organization system creates a MODELS directory within the specified output folder, automatically numbering and timestamping each generated file.

\subsection{Image to prompt component implementation}

\subsubsection{OpenAI vision API integration}

The Image to Prompt component utilizes OpenAI's GPT-4 Vision API to analyze images and generate detailed descriptions suitable for Text-to-CAD modeling. The component accepts two inputs: OpenAI API key for authentication and file path to the input image, producing a text description of how the 3D modeling should be done.

\begin{verbatim}
from openai import OpenAI
import base64

def encode_image(image_path):
    """Encode image file to base64 string"""
    with open(image_path, 'rb') as image_file:
        return base64.b64encode(image_file.read()).decode('utf-8')

# Initialize API connection
api_key = x
image_path = y

client = OpenAI(api_key=api_key)
base64_image = encode_image(image_path)

# Create vision API request
response = client.chat.completions.create(
    model='gpt-4o-mini',
    messages=[
        {
            'role': 'user',
            'content': [
                {
                    'type': 'text',
                    'text': '''Describe an object that can be modeled in CAD with 
                    simple operations, being as explicit as possible, using 
                    measures if possible and focusing on single, self-contained 
                    items rather than assemblies. Try to make descriptions as 
                    operations in a CAD software. Try not to build super long 
                    prompts.'''
                },
                {
                    'type': 'image_url',
                    'image_url': {
                        'url': f'data:image/png;base64,{base64_image}'
                    },
                }
            ],
        },
    ],
    max_tokens=300,
)

# Extract response content
a = response.choices[0].message.content
\end{verbatim}

The implementation uses base64 encoding to transmit images through the API, enabling support for various image formats including JPEG, PNG, and other common types. The system prompt is instructed to generate descriptions that focus on geometric primitives, dimensional specifications, and CAD-compatible operations.

\subsection{Error handling and validation}

\subsubsection{Input validation system}

The components also implement certain input validation to prevent common usage errors:

\begin{verbatim}
# API key validation
if not api or len(api.strip()) == 0:
    print("Error: API key is required")
    return

# File path validation
if not isinstance(step_file_path, str):
    print("Invalid path:", step_file_path)
    return []

if not os.path.exists(step_file_path):
    print("File does not exist:", step_file_path)
    return []

# Directory validation
if not os.path.exists(output_dir):
    os.makedirs(output_dir, exist_ok=True)
\end{verbatim}

This validation system checks for empty or invalid API keys, verifies file path existence and format, and automatically creates output directories when they don't exist.

\subsection{Usage guidelines and best practices}

\subsubsection{Text-to-CAD optimization}

For optimal results with the Text-to-CAD component, users should employ clear and specific geometric descriptions, include dimensional information when possible, focus on single objects rather than assemblies, specify material properties if relevant, and use standard engineering terminology. Effective prompts might describe "a cylindrical shaft with a diameter of 25mm and length of 100mm, with a 5mm keyway running along its entire length" or "a rectangular plate 50mm x 30mm x 5mm thick with four 6mm diameter mounting holes positioned 10mm from each corner."

\subsubsection{Image-to-Prompt effectiveness}

For effective image analysis, users should use high-quality, well-lit images, ensure objects are clearly visible, avoid cluttered backgrounds, include dimensional references when possible, and focus on manufacturable objects. The system works best with images that show single objects from clear viewpoints, preferably with some indication of scale or dimensional reference.

\subsection{Technical limitations and considerations}

The implementation shows several limitations that inform appropriate usage patterns. The Text-to-CAD component depends on external API availability and requires stable internet connectivity, while generation times vary significantly based on prompt complexity and service load. The Image-to-Prompt component may struggle with complex assemblies, abstract concepts, or images with poor lighting conditions.

Despite these limitations, the plugin successfully shows integration of AI capabilities into traditional CAD workflows, providing designers with new tools for rapid prototyping and concept development while maintaining compatibility with established parametric modeling practices.

\clearpage
\section{\textit{Ars Post Faber} Technical documentation}
